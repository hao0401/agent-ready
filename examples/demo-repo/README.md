# Acme Workbench

A small monorepo used to demonstrate Agent Ready.

## Apps

- `apps/web`: Vite React dashboard
- `services/api`: FastAPI service

## Commands

Use the package scripts in `package.json`.
